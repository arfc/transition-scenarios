<DeployInst>
    <prototypes>
        {%- for key, value in reactors.items() %}
        <val>{{ key }}</val>
        {%- endfor %}
    </prototypes>
    <build_times>
        {%- for key, value in reactors.items() %}
        <val>{{ value }}</val>
        {%- endfor %}
    </build_times>
    <n_build>
        {%- for key, value in reactors.items() %}
        <val>1</val>
        {%- endfor %}
    </n_build>
</DeployInst>