<DeployInst>
    <prototypes>
        <val>Sink_HLW</val>
        {%- for key, value in reactors.items() %}
        <val>{{key}}</val>
        {%- endfor %}
    </prototypes>
    <build_times>
        <val>900</val>
        {%- for key, value in reactors.items() %}
        <val>{{value}}</val>
        {%- endfor %}
    </build_times>
    <n_build>
        <val>1</val>
        {%- for key, value in reactors.items() %}
        <val>1</val>
        {%- endfor %}
    </n_build>
</DeployInst>
